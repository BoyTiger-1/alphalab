# local environment / secrets that are not part of the app
.env

# raw API downloads are a rebuildable cache (~50MB) — regenerate with tools/download_data.py
data/raw/
__pycache__/
*.pyc
*.log
.claude/
